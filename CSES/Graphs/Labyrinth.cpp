#include <bits/stdc++.h>

using namespace std;

int main ()
{
    int H, W, start, end;
    cin >> H >> W;
    int num_nodes = 1;
    vector<vector<int>> adj;
    vector<vector<bool>> not_wall (H, vector<bool> (W, false));
    char temp;
    for (int h = 0; h < H; h++)
    {
        for (int w = 0; w < W; w++)
        {
            cin >> temp;
            if (temp != '#') {
                icon 
            }
        }
    }
}